module.exports = {
  presets: [
    '@vue/cli-plugin-babel/preset'
    // Các preset khác (nếu có)
  ],
  plugins: [
    '@babel/plugin-transform-private-methods'
    // Các plugin khác (nếu có)
  ]
};